- name: Install docker on nextcloud VM
  hosts: nextcloud
  become: true
  roles:
    - role: docker
      tags: ['docker']

- name: Deploy nextcloud
  hosts: nextcloud
  become: true
  roles:
    - role: nextcloud_vm
      tags: ['nextcloud_vm']