apiVersion: v1
kind: Service
metadata:
  name: {{ nextcloud_ingress_service_name }}
  namespace: {{ nextcloud_namespace }}
spec:
  ports:
    - name: http
      port: 80
      targetPort: {{ nextcloud_http_port }}
      protocol: TCP
